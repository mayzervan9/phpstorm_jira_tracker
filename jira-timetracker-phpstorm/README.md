# Jira Time Tracker (PhpStorm plugin)

MVP IntelliJ Platform plugin for integrating with Jira and tracking worklogs.

## Dev

- Open this folder in IntelliJ IDEA / PhpStorm.
- Use Gradle tasks:
  - `runIde` to start a sandbox IDE with the plugin.
  - `buildPlugin` to build a distributable ZIP.
